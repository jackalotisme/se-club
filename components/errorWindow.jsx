import * as React from "react"

import { cn } from "@/lib/utils"
import { Input, stringify } from "postcss";

const ErrorWindow = React.forwardRef(({ className, placeholder }, ref) => {

    function handleChange(e) {
        let text = e.target.value;
    }
    return (
        ()
    );
})

export { ErrorWindow }
